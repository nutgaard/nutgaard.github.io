import React, { PropTypes as PT } from 'react';

function Header(){
    return (
        <div className="header dark">
            <h1>Utgaard</h1>
        </div>
    );
}

Header.propTypes = {};

export default Header;
